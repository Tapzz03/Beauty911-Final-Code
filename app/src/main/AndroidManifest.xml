<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Beauty911"
        android:name=".MyApplication"
        tools:targetApi="31">

        <!-- LoginActivity declared here -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Beauty911">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity declared here -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            tools:ignore="WrongManifestParent" />

        <!-- HomeActivity declaration added -->
        <activity
            android:name=".HomeActivity"
            android:exported="false" />

        <activity
            android:name=".RegisterActivity"
            android:exported="false" />

        <activity android:name=".HairServiceActivity" />
        <activity android:name=".NailsServiceActivity" />
        <activity android:name=".EyeLashServiceActivity" />
        <activity android:name=".MakeupServiceActivity" />
        <activity android:name=".FacialServiceActivity" />
        <activity android:name=".IVDripsServiceActivity" />
        <activity android:name=".PromotionsActivity" />
        <activity android:name=".CartActivity" />
        <activity android:name=".ResetActivity" />
        <activity android:name=".ConfirmationActivity" />
        <activity android:name=".IVDripConsentFormActivity"></activity>
        <activity android:name=".UserProfileActivity" />
        <activity android:name=".AboutActivity"></activity>
        <activity android:name=".CheckoutActivity"></activity>



    </application>

</manifest>
